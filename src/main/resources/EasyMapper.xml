<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.springframework.main.mybatis">
	<resultMap id="Easymap" type="org.springframework.main.mybatis.Easy">
		<!--id映射 -->
		<id property="eid" column="eid" />
		<!--普通属性映射 -->
		<result property="ename" column="ename" />
		<result property="epassword" column="epassword" />
	</resultMap>

	<select id="getById" parameterType="int" resultMap="Easymap">
        select *
        from Easy where eid = #{id}
	</select>
    
</mapper>